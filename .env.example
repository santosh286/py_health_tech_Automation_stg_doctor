# URL of the running Care Studio frontend
BASE_URL=http://localhost:3000

# Staging auth service (used for mocking in tests)
AUTH_SERVICE_URL=https://kapiva-auth-service-stg-170267861398.asia-south1.run.app/auth-service/api/v1

# Test credentials (only used for reference — tests mock the API by default)
TEST_USER_EMAIL=testuser@kapiva.in
TEST_USER_PASSWORD=yourpassword
